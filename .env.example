# Actian VectorAI — host and port of the running Docker container
VECTORAI_HOST=localhost:6574

# Collection name (created automatically on first run if it doesn't exist)
VECTORAI_COLLECTION=ui_removals

# Flask
FLASK_ENV=development
FLASK_DEBUG=1
PORT=5000
